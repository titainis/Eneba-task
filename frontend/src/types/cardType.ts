export type CardType = {
    id: number,
    title: string,
    imageUrl: string,
    platform: string,
    region: string,
    store: string,
    price: number,
    originalPrice: number,
    discountPercent: number,
    cashbackAmount: number,
    likesCount: number    
}